# 发布验收报告：PR #1 合并上线（2026-09-13）

## 发布记录

| 项 | 值 |
|---|---|
| 合并 PR | https://github.com/crash-you/laoliume/pull/1（merge commit） |
| 合并后 main SHA | `30a484e23b1456efb8e016daf5aa1a0c1063f8f8` |
| 合并前 main | `6f0edda5459c668b72ba129839484d0a9eac3275` |
| 部署方式 | Cloudflare Workers Builds **Git 自动部署**（合并即触发，非手动） |
| 部署版本 | Worker `laoliume` deployment `3255eac7-b0c8-4c78-9b43-1cb85d6f90b4`（2026-09-13T14:01:24Z） |
| 线上 build-commit | `30a484e23b1456efb8e016daf5aa1a0c1063f8f8`（与合并后 main 一致） |

## 验收结果（全部实测，非推断）

### Smoke（34/34 通过，exit 0）

`node scripts/seo-smoke.mjs --base-url https://www.laoliu.me --canonical-origin https://laoliu.me --env production --expected-commit 30a484e23b1456efb8e016daf5aa1a0c1063f8f8`

- 部署提交核验：线上 build-commit = 30a484e…（新版本已生效）
- 12 篇文章全部 200 + 标题/canonical/正文三段指纹一致
- 12 个无斜杠入口全部 308 单跳到规范路径
- 随机 404 真实 404；robots/sitemap/RSS 正常
- robots.txt：搜索爬虫允许 + **Sitemap 声明已恢复**（新部署后 Cloudflare 托管内容与源 robots 拼接生效；AI 爬虫 GPTBot/ClaudeBot/CCBot 被禁为站长既有策略）
- 资源魔数（PNG/SVG）与阅读量 API（X-Robots-Tag: noindex）均通过

### 补充验收（22/22 通过，exit 0）

- **OG 全引用**：从构建产物提取去重后 13 个 og/twitter 引用逐一 GET，全部 200 + PNG 魔数（apex 引用经 1 跳 301 到 www 后到达）
- **公众号入口**：线上文章含微信原文链接 +「首发于微信公众号」标识
- **跳转保留**：HTTP 与 HTTPS apex 带查询参数（?utm=test / ?x=1）均保留路径与参数、无循环，终点 200
- **随机 404（含查询参数变体）**：真实 404

### 已知域名方向问题（站长待办，非本次部署回归）

- apex `laoliu.me/*` 仍 301 → `www.laoliu.me/*`（Cloudflare 域级重定向），与代码 canonical（apex）方向相反
- 影响：canonical 指向的 apex URL 会多一跳 301 才能到达内容页；搜索引擎最终以 canonical（apex）为准，功能不受损，但浪费一跳、稀释抓取预算
- 修复方向（站长在 Cloudflare 后台）：删除「apex→www」重定向规则，改为「www→apex」308（保留路径与查询参数）；改完后重跑 smoke（base-url 换 https://laoliu.me）

## 回滚方案

- 代码：main reset 到 `6f0edda`，Workers Builds 会自动部署回滚版本
- 配置：本次未改任何 Cloudflare 设置/D1/路由——无需回滚配置
- 已知可用版本：部署 `a9ac310b`（2026-09-09，旧版站点）

## 剩余站长事项

1. **域名方向**（建议尽快）：Cloudflare → Redirects，把 apex→www 改为 www→apex 308
2. **GSC/Bing/百度**：提交 `https://laoliu.me/sitemap.xml`，观察索引
3. GA4 预览/本地隔离（非阻塞，见 docs/SEO_OWNER_ACTIONS.md 第 3 条）
